import { Chakra_Petch, Chonburi, IBM_Plex_Sans_Thai } from "next/font/google";

export const ibm = IBM_Plex_Sans_Thai({
  subsets: ["thai"],
  weight: ["400", "700"],
  display: "swap",
});

export const chakra = Chakra_Petch({
  subsets: ["thai"],
  weight: ["400", "700"],
  display: "swap",
});

import { } from "next/font/google";

export const chonburi = Chonburi({
  subsets: ["thai"],
  weight: "400",
});